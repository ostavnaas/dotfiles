#!/usr/bin/env bash
set -euo pipefail

VERSION="v4.2.0"
curl -L "https://get.helm.sh/helm-$VERSION-linux-amd64.tar.gz" -o helm.tar.gz
tar zxvf helm.tar.gz --strip-components=1 -C ~/.local/bin/ linux-amd64/helm && rm helm.tar.gz
